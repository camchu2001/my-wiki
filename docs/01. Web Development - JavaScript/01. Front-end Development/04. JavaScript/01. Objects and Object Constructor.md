There are multiple ways to define objects, but in most cases, it’s best to use **object literal** syntax. 
```js
const myObject = {
  property: 'Value!',
  otherProperty: 77,
  "obnoxious property": function() {
    // do stuff!
  }
};
```